# Requirements for Vision LLM (Qwen2.5-VL with OpenVINO)
# These are optional dependencies for vision-based document metadata extraction
#
# Installation:
#   pip install -r requirements-vision.txt
#
# Supported models:
#   - turingevo/Qwen2.5-VL-3B-Instruct-openvino-int4 (recommended, ~2GB)
#   - OpenVINO/Qwen2.5-VL-7B-Instruct-int4-ov (~4GB, better quality)

# Core OpenVINO runtime with optimum-intel
openvino>=2025.3
optimum[openvino]

# Transformers with Qwen2.5-VL support
transformers>=4.45.0

# PyTorch (CPU-only version to minimize size)
# Install separately: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
torchvision

# Image processing
Pillow>=10.0.0

# Download a pre-converted model:
# hf download turingevo/Qwen2.5-VL-3B-Instruct-openvino-int4 --local-dir models/Qwen2.5-VL-3B-Instruct-openvino-int4
